services:
  app:
    image: node:24-alpine
    working_dir: /app
    volumes:
      - .:/app
    command: >
      sh -c "npm ci && npm run build && npm run preview -- --host 0.0.0.0"
    ports:
      - "4173:4173"
